# Browser Smoke

Pending post-merge staging validation.

Required checks:

- compact Application Review rows render correctly
- `A — Corporate Entity Documents` preserved
- `B — Directors & UBO Identity Documents` preserved
- repetitive system-issue messaging reduced
- top action bar shows only primary actions directly
- portal behavior remains unchanged
