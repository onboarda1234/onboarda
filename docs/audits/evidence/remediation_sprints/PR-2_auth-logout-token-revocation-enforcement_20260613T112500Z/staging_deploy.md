# Staging Deploy

TBD.
# Staging Deploy

Pending. This branch has not been merged or deployed yet.

Required before closure:

- Merge PR-2 into `main`.
- Deploy merged `main` to staging.
- Record ECS task/image evidence, if available.
- Confirm `/api/version` `git_sha` equals merged main SHA.
- Confirm `/api/version` `image_tag` equals merged main SHA.
