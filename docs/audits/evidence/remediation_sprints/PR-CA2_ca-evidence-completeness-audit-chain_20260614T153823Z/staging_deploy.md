# PR-CA2 Staging Deploy Evidence

Status: pending.

This file must be completed after PR-CA2 is merged to `main` and deployed to staging.

Required evidence:

- Merged main SHA
- Deployment mechanism
- Deployment timestamp
- `/api/version` result with `git_sha` and `image_tag`
- Confirmation both values equal merged main SHA
