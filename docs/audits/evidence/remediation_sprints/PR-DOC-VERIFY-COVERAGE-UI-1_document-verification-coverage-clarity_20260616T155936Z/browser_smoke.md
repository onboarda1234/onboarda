# Browser Smoke

Pending merged-`main` staging validation.

Required checks:

- compact default rows
- no repeated issue/system-warning blocks
- `Details` collapsed by default
- verification coverage summary visible
- `Technical audit details` nested and collapsed
- direct `View` / `Download` / `Re-Verify` visibility
- approval blocker remains clear
