# PR-CA4 Staging Deploy Evidence

Status: pending.

This branch has not yet been merged to `main` or deployed to staging from this working session.

Required post-merge evidence:

- Pull latest `main`.
- Record merged main SHA.
- Deploy merged main to staging.
- Confirm staging `/api/version` returns both `git_sha` and `image_tag` equal to the merged main SHA.
- Run staging API smoke.
- Run staging browser smoke because this PR changes officer-facing UI.

No staging deployment is claimed in this evidence pack.
