# Staging Deploy

Status: PENDING.

PR-1C has not yet been merged or deployed.

Closure requirement:
- Merge PR-1C to `main`.
- Deploy merged `main` to staging.
- Confirm staging `/api/version` returns `git_sha` and `image_tag` equal to the PR-1C merged main SHA.
