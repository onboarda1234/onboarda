# ARIE Finance — Python Dependencies (pinned for reproducible builds)
# ═══════════════════════════════════════════════════════════
# Core Framework
tornado==6.5.5

# Authentication & Security
bcrypt==5.0.0
PyJWT==2.12.1
cryptography==46.0.5

# Data Validation
pydantic==2.12.5
typing_extensions==4.15.0

# HTTP Client (for external API integrations)
requests==2.32.5

# Anthropic Claude AI SDK (document verification, memo generation)
anthropic==0.49.0

# Environment & Configuration
python-dotenv==1.2.2

# Production WSGI Server (use with reverse proxy)
gunicorn==25.1.0

# PostgreSQL Support (production database)
psycopg2-binary==2.9.11

# AWS S3 Support (document storage)
boto3==1.42.73

# Observability & Monitoring
prometheus-client==0.24.1

# System Health Monitoring (disk/memory in health endpoint)
psutil==7.2.2

# Async SQLite support
aiosqlite==0.22.1

# Server-side PDF Generation
weasyprint==68.1

# Country code normalisation (ISO 3166 alpha-2 → alpha-3)
pycountry==26.2.16

# NOTE (BSA-017): test/dev-only dependencies live in requirements-dev.txt —
# production images must install ONLY this file.

