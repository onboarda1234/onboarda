# Deprecated Render blueprint (intentionally non-deployable).
#
# Canonical deployment config lives at repository root:
#   ../render.yaml
#
# Keeping this file empty prevents accidental deployment from arie-backend/
# with stale settings.
services: []
